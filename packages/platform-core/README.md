# @anan/platform-core

Shared platform primitives for Anan apps.

This package holds pure cross-app helpers and contracts for auth bridge setup,
domain errors, session projections, locale handling, Convex API adapter helpers,
and class name merging.
