# @anan/web-foundation

Shared web app foundation helpers. App wrappers still own cookies, redirects,
font source paths, and route defaults.

Exports include API response helpers, auth-session projection helpers, shared
font/locale utilities, media helpers, theme provider, and Convex provider
factories.
