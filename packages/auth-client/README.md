# @anan/auth-client

Browser Better Auth client presets for web, admin, and external apps.

App wrappers select `web`, `admin`, or `external apps` presets. Form adapters live in
`@anan/auth-client/forms` so UI code does not depend on Better Auth internals.
