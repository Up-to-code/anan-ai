# MVP Data Layer

Deterministic guest-mode logic for the chat-first mobile app.

- `ananCatalog.ts` defines the buyer-facing property catalog and capability metadata.
- `ananAssistant.ts` converts prompts into typed conversation blocks without live AI wiring.
- `formatters.ts` centralizes Arabic-friendly formatting helpers.
