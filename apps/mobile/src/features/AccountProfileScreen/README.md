# AccountProfileScreen

Focused buyer profile editor for local-device identity details.

- Reads and writes through `useBuyerAccount`.
- Keeps live authenticated identity as the higher-trust source while still allowing local device edits.
