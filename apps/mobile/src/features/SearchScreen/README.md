# SearchScreen

Focused buyer search surface in a normal marketplace layout.

- `index.tsx` renders the search shell, assistant context card, filters, and result list orchestration.
- `SearchResultCard.tsx` owns the marketplace result card used only by this screen.
- `usePropertySearch.ts` owns the client-side filter state.
