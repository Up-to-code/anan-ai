# GalleryScreen

Immersive property media screen for the buyer mobile journey.

- `index.tsx` orchestrates property loading, active image state, and the bottom journey actions.
- `GalleryViewport.tsx` owns the paged full-screen image stage.
- `GalleryThumbnailRail.tsx` renders the compact thumbnail navigator for fast image switching.
