# Properties Page

- Combines inventory filters, create/edit form, and paginated property list.
- Uses query-string selection instead of a separate detail route for v1.
- Supports create, update, and delete against the existing admin property handlers.
