# Orders Page

- Shows operational order records with status filtering.
- Lets admins update status, assignee, and notes inline.
- Keeps v1 workflow lightweight by using one table with per-row update forms.
