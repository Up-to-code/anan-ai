# PropertyDetailPage

Owns the buyer-facing property detail route used from assistant results and landing previews.

The page reads live property detail from `convex/user_zone/web/properties.ts` and keeps the action set focused on:

- returning to the assistant
- opening a finance prompt
- requesting advisor handoff
