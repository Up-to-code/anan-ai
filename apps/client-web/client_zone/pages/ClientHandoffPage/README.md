# ClientHandoffPage

Owns the post-handoff confirmation route.

The page intentionally stays small in version 1:

- confirms the advisor handoff status
- references the last local handoff snapshot when available
- sends the user back to the assistant or discovery flow
