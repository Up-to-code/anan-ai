# Docs Components

Shared rendering components for the public developer docs inside the `web` app.

## Purpose
- Keep `/docs/*` pages thin.
- Centralize the docs shell, article renderer, code blocks, endpoint cards, and navigation.
- Pair with `apps/web/app/(public)/docs/_components/ui` for docs-specific UI primitives.
