# Public Zone Components

Shared public-site components that belong to the `(public)` zone.

- Marketing and legal routes import these through `@/app/(public)/public`.
- Do not use this folder as a cross-app shared bucket.
