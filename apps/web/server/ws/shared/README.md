## Workspace Shared Helpers

This folder owns internal helpers shared by multiple workspace capabilities.

- `errors.ts`: unavailable-zone guardrails for unsupported audiences.

Keep this folder internal to `apps/web/server/ws`.
