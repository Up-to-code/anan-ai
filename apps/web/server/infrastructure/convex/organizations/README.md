## Organizations Convex Adapters

This folder owns organization-scoped Convex adapters for the web server layer.

- `index.ts`: current-user organization management and directory flows
- `apiKeys/`: manager and machine API-key operations
- `assets/`: organization-owned asset reads and attachment flows

Keep organization transport details here so domain services stay focused on permissions and business rules.
