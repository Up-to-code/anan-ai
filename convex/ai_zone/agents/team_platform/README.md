# team_platform

This team exists to answer **platform and backend architecture questions** for workspace engineers.

Key constraints:

- Uses a **secret-free** developer handbook knowledge store (`developerHandbookPages`).
- Must be dispatched only for platform/architecture/security/performance questions (not for normal user property flows).
- Output should be rule-based and point to file paths and checklists.

