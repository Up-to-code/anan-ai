## Shared Property Helpers

This folder holds shared property-domain helpers used across zones.

- keep derived persistence helpers here
- avoid deep imports from `admin_zone` into Broker/RED logic
- keep these helpers free of role/session orchestration
