# Batch Utilities

This folder hosts shared batch-processing helpers wired to Convex components.

- `index.ts` exposes a reusable `BatchProcessor` instance.
- Future batch jobs should live beside their zone logic and import from here.
